function test(){
  // Create a new paragraph element
  const paragraph = document.createElement('p');
  paragraph.textContent = 'Set Goal button was pressed.';

  document.body.appendChild(paragraph);
}